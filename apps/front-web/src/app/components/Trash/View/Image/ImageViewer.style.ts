import styled from 'styled-components';

export const StyledImageViewer = styled.img`
  width: 100%;
  height: 20rem;
  height: '100%';
  object-fit: cover;
  box-shadow: 0 0 10px 0 rgba(0, 0, 0, 0.2);
  object-position: center;
  margin-bottom: -8px;
`;
